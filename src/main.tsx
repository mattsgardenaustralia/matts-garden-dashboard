import React from 'react';
import ReactDOM from 'react-dom/client';
import { PasswordGate } from './PasswordGate';
import './styles.css';
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><PasswordGate /></React.StrictMode>);
